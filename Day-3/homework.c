#include <stdio.h>

void main() {
    int x, y, z, a, b, c;
    printf("Enter value of x, y, z, a, b and c: ");
    scanf("%d %d %d %d %d %d", &x, &y, &z, &a, &b, &c);

    if (x < 10)
    if (y > 20)
    if (z < 30)
    if (a > 40)
    if (b < 50)
    if (c > 60)
        printf("A");
    else
        printf("B");
    else
        printf("C");
    else
        printf("D");
    else
        printf("E");
    else
        printf("F");
    else
        printf("G");
        printf("H");
}
